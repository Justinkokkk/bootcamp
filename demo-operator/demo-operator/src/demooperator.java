public class demooperator {
  public static void main (String [] args) {
    //x++; // same as x=x+1 and x+=1
    // ++x // same but with little difference.
    int y = 5 ;
    
    y--;
    System.out.println(y);
    y-=1;
    y+=1;
    System.out.println(y);
    y-=1;
    
    System.out.println(y);

    // *=, /=, %=

    // >,<,<=,>=
    
    // and (&&) , or (||
    
    //==,!=

  }
}